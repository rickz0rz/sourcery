# Build output
/sourcery
/dist/

# Local device configuration. Copy config.example.json to config.json and fill
# in your own devices; the result is yours and stays out of the repository.
config.json
config.local.json
